<%= form_with(model: collection_log_item) do |form| %>
  <% if collection_log_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(collection_log_item.errors.count, "error") %> prohibited this collection_log_item from being saved:</h2>

      <ul>
        <% collection_log_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
